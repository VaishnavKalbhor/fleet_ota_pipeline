apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: climate-control
  namespace: fleet-ota
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: climate-control
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
